# Base URL of the NestJS backend API.
# Local dev default (when unset): http://localhost:5000
# Production (Render): https://your-backend.onrender.com  (no trailing slash)
VITE_API_URL=http://localhost:5000
